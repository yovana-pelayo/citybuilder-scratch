// export function makeStatsString(landCount, homeCount, mealCount) {
//     return `You clicked on the land ${landCount} times, on the home ${homeCount} times, and on meal ${mealCount} time. And everybody loves your quotes`;
// }